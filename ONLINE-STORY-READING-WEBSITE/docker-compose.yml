services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6335:6333"   # REST API
      - "6336:6334"   # gRPC
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_storage:
